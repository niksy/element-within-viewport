{{> top title="Callback, single element"}}

<button type="button" class="destroy">Destroy instance</button>

<div class="block"></div>
<div class="block"></div>
<div class="block"></div>
<div class="block"></div>
<div class="block"></div>
<div class="block"></div>
<div class="block"></div>

<script>

	$('.block').eq(0).inViewport({
		threshold: 300,
		success: function ( el ) {
			console.log( el.index() );
		}
	}).html('Plugin initialized');

	$('.block').eq(0).inViewport({
		threshold: 300,
		success: function ( el ) {
			console.log( 'Second time!' );
		}
	}).html('Plugin initialized');

	$('.block').eq(0).inViewport({
		threshold: 300,
		success: function ( el ) {
			console.log( 'Third time!' );
		}
	}).html('Plugin initialized');

	$('.destroy').on('click', function () {
		$('.block').eq(0).inViewport('destroy').html('Instance destroyed');
	});

</script>

{{> bottom}}
