{{> top title="Callback, multiple elements"}}

<button type="button" class="destroy">Destroy instance</button>

<div class="block"></div>
<div class="block"></div>
<div class="block"></div>
<div class="block"></div>
<div class="block"></div>
<div class="block"></div>
<div class="block"></div>

<script>

	$('.block').inViewport({
		threshold: 300,
		success: function ( el ) {
			console.log( el );
		}
	}).html('Plugin initialized');

	$('.block').inViewport({
		threshold: 300,
		success: function ( el ) {
			console.log( 'Second time!' );
		}
	}).html('Plugin initialized');

	$('.block').inViewport({
		threshold: 300,
		success: function ( el ) {
			console.log( 'Third time!' );
		}
	}).html('Plugin initialized');

	$('.destroy').on('click', function () {
		$('.block').inViewport('destroy').html('Instance destroyed');
	});

</script>

{{> bottom}}
